InnerSelf("context");
const modifier = (text) => {
  // Append Author's Note from NGO if desired
  if (state.originalAuthorsNote && state.storyTemperature > 5) {
      text = text + "\n\n[Author's Note: " + state.originalAuthorsNote + "]";
  }
  return { text, stop }; 
};
modifier(text);
